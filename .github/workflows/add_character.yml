name: Auto Create PR from Issue

on:
  issues:
    types: [opened, edited]

jobs:
  create-pr:
    # Workflow se spustí pouze tehdy, pokud má Issue správný nadpis nebo label
    # Doporučuji v novém formuláři (yaml) nastavit fixní label, např. "new-character"
    if: contains(github.event.issue.labels.*.name, 'new-character') || contains(github.event.issue.title, 'Add character')
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pull-requests: write

    steps:
      # 1. Klonování repozitáře
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Nastavení Pythonu
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. Spuštění parsovacího skriptu s předáním textu z Issue
      - name: Parse Issue and Generate JSON
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: python code/parse_issue.py

      # 4. Vytvoření Pull Requestu pomocí hotové akce
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "feat: add new character from Issue #${{ github.event.issue.number }}"
          branch: "add-character/issue-${{ github.event.issue.number }}"
          title: "Přidání postavy z Issue #${{ github.event.issue.number }}"
          body: |
            Tento Pull Request byl automaticky generován na základě formuláře v Issue #${{ github.event.issue.number }}.
            Prosil bych o kontrolu dat před schválením.
          labels: |
            automated-pr
            data-update
